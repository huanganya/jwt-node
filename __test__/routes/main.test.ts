describe("main router", () => {
  describe("get /someendpoint", () => {
    afterEach(jest.restoreAllMocks);

    it("should response with OK status", async () => {
      //this is the test for someendpoint
    });
  });
});
